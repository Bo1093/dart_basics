void main(List<String> args) {
  const age = 64;
  const age2 = 56;
  print(age + age2);
  print(age / age2);
  // By default / considers double. hence to use Integer divison:
  print(age ~/ age2); //Truncating division operator
  //
}
